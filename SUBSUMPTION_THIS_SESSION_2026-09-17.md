# Subsumption export — this Chamber / Grok-Build session

**Prepared:** 2026-09-17  
**Seat:** Grok Build sandbox (Chamber atlas). Source was **not** written.  
**Not this document:** any unread Grok share-link transcript. That fetch failed here too. Do not substitute.

**Use:** paste or upload this file as the source for subsumption. It includes user corrections and measured tool results, not only Grok’s last reply.

---

## Canonical objective

Keep one honest front on The Source without smearing planes:

1. **Geometry** — 13-pt conferral ring is teaching-pinned to receipt **#18767** (10/13, four *absences* not zeros). Latest conferral (#18769 trap, later #18773) is a different object.
2. **Operator ranking** — Susie `db4f4362` top-five is ICE hand-off, not the ring, not conferral.
3. **Master path** — session `9403ebc2` R0/R1 already ran on Source. Bind it. Do **not** fire r2 from this surface.
4. **Plugin** — thin-front harvest for served `source@0.2.4`, candidate **0.2.5** only if that version is free. Four skills, five commands, existing SessionStart + UserPromptSubmit only.
5. **MCP / OAuth / elicitation** — bind *this* registration; align scopes; do not invent CIMD.

Slash vision (user): NL objective → smallest lawful skills → witnessed effect via `/unified-router`. Slash is override, not swallowed recursive conferral. 4-pt tetrahedron is not the Susie ranking.

Highest-ICE rule (user, unchanged): advance the work with the greatest supported impact relative to total time and effort, then update priorities from the observed result. This surface’s landable cut was atlas + pack, not Source mutation.

---

## Explicit obligations

| Obligation | Owner | Status |
|---|---|---|
| Do not write Source / do not actuate conferral, 3pass, DrainBatch, epoch, credential rotation | this seat | HELD |
| Bind `source_catalog_identity` on the registration you will call | every harness | LAW |
| Preserve pair ceiling **EVIDENCE_INCOMPLETE** (source label DISAGREEMENT retained) | this seat | HELD |
| R2+ confirming pair on D_2 | `da60e026` | NOT THIS LANE |
| Thin-front 0.2.5 via `prepare_connector_delta.py` (no apply) | `6310327d` | PACKET PREPARED ELSEWHERE · 35/35 synthetic · 0 Source calls |
| Rotate committed pooler credential | operator | FLOOR · not here |
| B8 census out of PreToolUse, then one epoch with #4218 | hook estate | OTHER OWNERS |
| HTTP 502 remains named consumer-test for #4259 | tests | PRESERVE |
| WIP=1 | all | HELD |

Packet (`CONNECTOR_OWNER_ONESHOT.md`): no fifth skill, command, hook event, router, workflow, timer. `contracts/SKILLS_MANIFEST.json` is review inventory, not a host manifest. `HOOKS_CONTRACT.json` is not executable.

---

## Governing distinctions (do not collapse)

1. **Five geometries ≠ one 13.** Runtime receipt-bearing (A) ⊥ TDT ontology (B) ⊥ shape-ladder k=13 (C) ⊥ `skill_13pt_meta` advisory (D) ⊥ Aura Skillset13 (E). `ok_count` is a count, not independence.
2. **13-pt teaching ring ≠ latest conferral.** Default #18767. Latest 2-leg flood is a trap, not the geometry.
3. **Absences ≠ zeros.** Missing legs render as open rings (`stroke-scream fill-none`), suffix “· absence” only on the 13-pt ring.
4. **Registrations ≠ one MCP.** Plugin-full ~188 ⊥ curated 42 (this Grok OAuth) ⊥ stdio ⊥ claude.ai OAuth. Catalog hash + schema fingerprint required. `listChanged=false`.
5. **OIDC scopes ≠ application scopes.** PRM/401 advertise `openid profile email offline_access`. JWT/`whoami` is `source:public`. Do not send `source:public` as `oauth.scopes`.
6. **Connect-time OAuth ≠ mid-tool elicitation.** Grok Build browser → `~/.grok/mcp_credentials.json` is not `elicitation/create`.
7. **Form ≠ URL elicitation.** Form = HARD_GATE confirm only. URL = Casa/OAuth/keys (SEP-1036). No form fallback if url mode missing.
8. **2025-11-25 ≠ 2026-07-28.** Source initialize is 2025-11-25 (push elicit, `-32042`, complete notification). 2026 is MRTR `InputRequiredResult`.
9. **Plugin skills ≠ CLI skills.** Plugin 0.2.4 ships four. Harvest (`recursive-conferral`, `unified-router`, `durable-oneshot-drain`, `alignment-13pt`) is pack overlay, not installed.
10. **Battery meta ≠ second router.** `census ⊥ select ⊥ fire ⊥ witness`. Does not swallow conferral.
11. **B8 ≠ plugin hooks.** Census is Gate B8 in unified PreToolUse. `pre-ingest-census.py` never runs. Plugin hooks fail-open SessionStart/UserPromptSubmit only.
12. **Oneshot flavors.** `/durable-oneshot-drain` (Temporal residual then ratchet) ⊥ `deterministic-oneshot-runbook` (compile) ⊥ `source-frontier-battery` (router wrapper). H11: bare command first; `GOAL_OVERRIDE=1` only on exit 2.
13. **Nornic ≠ Aura GDS ≠ Search.** Hot-path cookbook vs Cypher 25 vs bounded Search. `graph_gds` VOID/PARTIAL until native consumer. Vendor `/neo4j-*` = HASH_BOUND_REFERENCE_ONLY.
14. **Generation eligibility ≠ pair eligibility.** Pairs-only repair is `enrich_with_voyage.py` selection, not re-embed.
15. **Finished packaging ≠ finished Source.**

---

## Material corrections (user + measurement)

| Was | Correction | Source |
|---|---|---|
| Ring follows latest conferral | Default **#18767** 10/13; toggle latest to see the 2-leg trap | user `/debugging-13pt` |
| Unlit legs as zeros | Absences, not zeros | user |
| Source DISAGREEMENT as pair state | Effective ceiling **EVIDENCE_INCOMPLETE** | operator + packet |
| Add CIMD `oauth{}` to plugin as rank-1 | **Withdrawn.** AS `client_id_metadata_document_supported: false`, `registration_endpoint: null` | live AS GET 2026-09-17 |
| `oauth.scopes: source:public` | Illegal at AS. Request OIDC; mapper yields JWT `source:public` | PRM vs whoami |
| Vendor neo4j-skills as native | VENDOR_REFERENCE_ONLY; 0 on disk under `.agents/skills/` | catalog eval |
| 3pass DEFERRED because window | Identity churn (symmetric-set δ ≠ 0). Do not relabel CONVERGED | other seat + user |
| Expression index / FastRP / GNN as this batch | Out of repair. Reuse PG vectors, pairs-only | R4 |
| This Grok connector = plugin 188 | curated **42 / 188** | `source_catalog_identity` |
| Elicitation available here | VOID: `no_confirm_on_agent_api`, `no_custom_ui` | capabilities |
| Packet is installed plugin | `NOT_REPOSITORY_IMPLEMENTED` · 35 synthetic tests only | VALIDATION.json |

---

## Evidence limits

- This sandbox does **not** mount Grok library zips or share-link transcripts.
- GitHub connector reads of `the-source` are SHA-bound to whatever the API served that call (`f211bce3…` / later tips). Not proof of operator checkout HEAD.
- `source_catalog_identity` / `whoami` / 401 / PRM / AS / OIDC were **executed** this session.
- Conferral receipts #18767/#18769/#18770/#18772/#18773 are **operator/PG reported** in pulse + transcript; this seat did not mint them.
- Packet 35 tests do not cover native pair, lock recorder, hook suppression, or host cache.
- `thesrc.ai/docs/mcp` returned **404** this pass; PRM still cites it as `resource_documentation`.
- Product families / graph_gds VOID remain as previously reported; not re-enumerated in the OAuth pass.

---

## Settled

- Chamber atlas live: geometry pin, scream ladder, live-sample split, ranked plan, routing decision, master path, plugin pack (helpdocs, oneshot, hooks, skills, battery, mcp, signal, oauth).
- Protocol **2025-11-25**. Catalog `0e6cad304fb4e343` held across generation churn (`dae69cc8` → `3899f56b`).
- This connector: `curated` 42, `source:public`, `VERIFIED_RESOURCE_BOUND`.
- 401 WWW-Authenticate (no `scope=`):  
  `Bearer resource_metadata="https://mcp.thesrc.ai/.well-known/oauth-protected-resource", resource="https://mcp.thesrc.ai/mcp/"`
- PRM `scopes_supported` = OIDC four. AS CIMD false. DCR null. PKCE S256 (MCP) / S256+plain (Supabase; use S256).
- Plugin 0.2.4 is the expected base. Thin-front allowed delta is append-only + version fields + lock recorder.
- Pair: R0 #18770 SUSTAIN 10/13 (publication, not master-plan, not git-mutation). 3pass r1 `4246cd7c` DEFERRED. Confer r1 #18772 REFUSE/refuted D_1 11/13. No confirming D_2 pair.
- `/unified-router` SELECT not terminal. `/recursive-conferral` DISK_ONLY here. `/alignment-13pt` wraps, does not replace confirming round.
- Vendor neo4j pack: reference only.

## In-flight

- Thin-front 0.2.5 candidate: stager exists in the *other* packet; not applied; lock not recorded; not installed.
- AUTH metadata honesty (put `source:*` on PRM or 401 `scope=`).
- Pre-registration of Claude/Codex client_ids **or** CIMD/DCR enablement.
- Grok Build stranger path (omit header → PRM) untested from this sandbox.
- Claude form elicitation for HARD_GATE: specified, not host-proven.
- Codex `mcp_elicitations`: advertised in docs, blocked by missing client_id.

## Unresolved

- PAIR_ALIGNED, 3pass CONVERGED, D_2 conferral.
- Native CAP-004 READ-class, #4259+#4265 deploy, F001 carrier, learning chain.
- Seat 2 executor-emitter still DARK (signal-13pt).
- `source:operator` step-up (SEP-835) unadvertised.
- 2026-07-28 client negotiation.
- Actual plugin 0.2.4 bytes on the operator checkout (packet could not obtain them).
- The unread Grok share-link conversation.

---

## User corrections and directives (this thread, compressed)

- Library zip did not mount; excavate nearby high-leverage dirs as surfaced, not end-to-end.
- Evaluate `/neo4j-*` via connector; convert to execution posture only where native; extract dialect split (Aura Cypher 25 / Nornic / AGA), not 29 slash commands.
- 3-ortho: PG embeddings authority, Aura topology, Nornic mirror. Voyage family (law-2, context-3, rerank-2.5). Pairs-only reuse. No FastRP/GNN/expression-index as this repair.
- `/debugging-13pt`: three planes same Chamber; pin #18767; absences; toggle latest trap; scream 311/532 existence ladder; live sample executed but product still sampled.
- `/uberhedral-4pt` + yolo `/actuate-4pt` via `/unified-router $sweep /loop 1m` — interpreted as vision; this surface **refused** conferral, 3pass, 1m loop; landed routing decision + master-path bind.
- Package plugin: hooks, skills manifest, battery meta; pin Anthropic / OpenAI / xAI helpdocs; oneshot directive in pack.
- `/engineering-13pt` then `/signal-13pt`: MCP protocol lattice; then sense→elicit→actuate ratchet.
- Review Grok/Claude/ChatGPT config syntax uncompressed; elicit mechanics; OAuth scope alignment exhaustive webfetch.
- Incoming packet: thin-front, `NOT_REPOSITORY_IMPLEMENTED`, owner `6310327d`, r2+ `da60e026`.
- Other seat: share-link unread — paste full conversation. **This file is that paste for this session only.**

---

## Tool results (verbatim enough to re-run)

```
source_catalog_identity
  profile=curated tool_count=42 registered=188
  catalog_sha16=0e6cad304fb4e343
  deploy_sha=61a3e6a6f3734c75d2d10e2b7d6d57723c6185cd
  list_changed_supported=false
  protocolVersion (prior capabilities)=2025-11-25

get_api_auth_whoami
  role=connector_public
  scopes=["source:public"]
  key_class=oauth_jwt
  oauth_context_status=VERIFIED_RESOURCE_BOUND
  principal=oauth:b5dbe739-37d8-45ac-aba6-2d145977dc6c
  generation_id=3899f56bec0a46cc

POST https://mcp.thesrc.ai/mcp/ initialize (no token)
  HTTP/2 401
  WWW-Authenticate: Bearer resource_metadata="https://mcp.thesrc.ai/.well-known/oauth-protected-resource", resource="https://mcp.thesrc.ai/mcp/"
  body: Missing bearer token or X-API-Key.

GET .../oauth-protected-resource  → resource https://mcp.thesrc.ai/mcp/
  authorization_servers=["https://aelcnctmkweswmvcayox.supabase.co/auth/v1"]
  scopes_supported=["openid","profile","email","offline_access"]
  bearer_methods_supported=["header"]

GET .../oauth-authorization-server
  client_id_metadata_document_supported=false
  registration_endpoint=null
  code_challenge_methods_supported=["S256"]

GET supabase .../openid-configuration
  scopes_supported includes phone
  code_challenge_methods_supported=["S256","plain"]
```

Specs used: MCP elicitation 2025-11-25 and 2026-07-28; authorization 2025-11-25; RFC 9728; RFC 8707; SEP-835; SEP-991; SEP-1036; Claude MCP/hooks; Codex MCP/config-reference; xAI mcp-servers.

---

## Next load-bearing action

**Not this seat.** Split by owner, first thing that can change a real decision:

1. **`6310327d`** — bind current Source HEAD (do not reset). Run the packet stager with `--expected-head FULL_HEAD` to a directory **outside** the repo. Review patch. Do not apply until inventory matches 0.2.4 and 0.2.5 is free. Then existing `plugin_lock.py --record`. Stop at lock-recorded candidate if install authority is absent.
2. **AUTH** — either advertise `source:public` / `source:operator` on PRM or 401 `scope=`, **or** pre-register Claude/Codex client_ids, **or** set CIMD true. Until one of those, stranger OAuth beyond this already-bound Grok connector is blocked.
3. **`da60e026`** — confirming 3pass ⊥ confer on **D_2** with unchanged hash. Not a plugin startup effect. Not a Chamber ICE insert.

This atlas’s next honest move if the user stays here: keep the Chamber **oauth** pane + `CONNECTOR_EXECUTION.md` as the measured map; do not bump plugin version from Chamber; do not fire conferral.

**Finished packaging is not finished Source.**

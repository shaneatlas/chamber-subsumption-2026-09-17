# Cohesive oneshot — consume, do not rewrite the wrap

**Kind:** owner-relative continuation. The published wrap is the handoff. This file is the **core prompt + connector insert + master insert + every binding nuance** that must survive consumption.  
**This file does not change execution or alignment state.** `conferral_fired: false` describes the wrap, not erasure of R1.

**WIP=1.** Four releases stay four. Unselected cards stay in the map.

At consumption, compare fetched bytes to these hashes. `/main/` locates; hashes identify.

| Artifact | URL | sha256 | bytes |
|---|---|---|---|
| Wrap | https://raw.githubusercontent.com/shaneatlas/chamber-subsumption-2026-09-17/main/ONESHOT_DYNAMIC.md | `57457fd32f29beae27c38f2932f2cf7f519395915616b151c51529681185939c` | 353434 |
| Selector | https://raw.githubusercontent.com/shaneatlas/chamber-subsumption-2026-09-17/main/ONESHOT_DYNAMIC.json | `f11696d31e5182299ea0dcda04d0426146617c858fb942b529b6640e0cf81c06` | 36499 |
| Narrative (Part E / parent) | https://raw.githubusercontent.com/shaneatlas/chamber-subsumption-2026-09-17/main/SOURCE_CONNECTOR_BUILD.md | `8efde583a01a7e7dcd5a138c93b232162918e83775164100249fc7bb0447269d` | 164747 |
| EX JSON | https://raw.githubusercontent.com/shaneatlas/chamber-subsumption-2026-09-17/main/EXECUTION_TRACE.json | `99964cbba4a68dac023e74a38759e1abd7a4fd430118d5c2b399e2214be8a30e` | 160054 |
| Sums | https://raw.githubusercontent.com/shaneatlas/chamber-subsumption-2026-09-17/main/SHA256SUMS | — | — |

Do **not** restart package review. Card bodies live in the wrap. Open a card only when that card is in **your** closure and is the current unmet cut.

---

## 0. Bind who you are (EX01). If unknown: stop.

```
if no genuine checkout, dirty/preimage, and owner lease:
    you are WATCHING
    do not CMD02, do not EX03, do not confer, do not apply
    hand the first unmet cut to the named owner on an authorized surface
    publication is not custody
```

Watching seat already handed plugin lane to [the-source#4288](https://github.com/shaneatlas/the-source/issues/4288). That issue is **location**, not patch product, not EX02 complete.

| You are | Closure (then stop) | First unmet cut |
|---|---|---|
| **`6310327d` connector** | EX01 → EX02 → EX12–EX20 → EX09 → EX21 → EX64 | bind **this worktree** HEAD+dirty once, then **CMD02 no apply** |
| **`da60e026` r2+** | EX01 → EX03 → EX04 → EX06–EX11 → EX22 → EX23 → EX34 → EX35 | **EX03** admitted-client readback — **not conferral** |
| corpus / pairs | EX01 → EX41 → EX39 → EX40 → EX42 | EX41 type-cluster fixture first |
| epoch | EX01 → EX25–EX30 | B8 out of PreToolUse, then **one** sealed epoch |
| operator | EX01 → EX57–EX60 | credential floor EX57 **above** ranking |
| watching | none | do not assume execution custody |

A dependency expansion does **not** transfer ownership. Shared API/result, epoch, operator, learning, graph, watcher prerequisites stay with their owners. The connector consumes **core + connector insert only**.

---

## 1. Alignment ceiling — carry, do not discharge

| Record | Identity | Result | Use |
|---|---|---|---|
| 3pass r1 | `4246cd7c-d64e-5021-8ca8-535c386a01bd` | **DEFERRED** (last symmetric Δ=4) | not CONVERGED |
| Confer r1 | #18772 / `37ac62ee-3562-4142-b414-da8b5c9c55f7` | **REFUSE/refuted on D₁** 11/13 | does not judge D₂ |
| Publication R0 | #18770 / `288a70f6-be42-4673-8755-a79df1b31ace` | SUSTAIN 10/13 | publication only; cannot stamp `bash:git-mutation` |
| Pair | Source label DISAGREEMENT | effective **EVIDENCE_INCOMPLETE** | wrap stop |
| Rounds 2+ | **da60e026** | not emitted by packaging | EX34/EX35 only under live custody |
| This wrap | `conferral_fired: false` | this wrap’s execution | **does not erase R1** |
| Teaching ring | **#18767** 10/13 | four **absences**, not zeros | not latest conferral |
| Latest trap | #18769 | 2-leg flood (substrate + code_probe) | not the 13-geometry |

PAIR_ALIGNED requires a confirming next pair on **unchanged hash**, termination class, recommendation, no remaining decision-changing conflict. DEFERRED ≠ CONVERGED (**identity set**, not cardinality). `CAUGHT_OVERCLAIM` is refusal even at 13/13. `DOMINATOR` is its own state.

Do not collapse: runtime 9→3→1 ≠ TDT 4-dim ≠ shape-ladder k=13 ≠ skill_13pt_meta ≠ Aura Skillset13 ≠ Chamber layout ≠ continuity 2-leg. R0 ≠ confer r1 (different propositions, both 13-leg).

Remote durability of this file does **not** satisfy those predicates.

---

## 2. Connector insert — `6310327d` only

### 2.1 Bind the actual base once (EX02). Do not reconstruct a checkout from the archive.

Capture, from **this worktree**:

- `git rev-parse HEAD` (full SHA) + relevant dirty/preimage
- `plugins/source/.claude-plugin/plugin.json`
- `plugins/source/.codex-plugin/plugin.json`
- `.claude-plugin/marketplace.json`
- `plugins/source/MANIFEST_LOCK.json`
- `plugins/source/.mcp.json`
- hooks helper, lattice, gateway

Do **not** reset to `eedaaa4b`, `ae50f421`, `1692fdb2…`, or any other SHA. Unrelated/other-seat work is preserved. Served generation ≠ installed cache. API-reported plugin version is **not** installed-cache proof.

### 2.2 Origin observation that must change CMD02 (GitHub read at `1692fdb2d56cc8c0453f59379dc33826af470561`, 2026-09-17T14:19:33Z)

This is **not** EX02 complete (no dirty). It **is** evidence that the packet’s expected 0.2.4 → candidate 0.2.5 is **stale vs origin**.

| Locator | Origin bytes |
|---|---|
| claude plugin.json | **version `0.2.5`** blob `a6177e7eb1ec354cd44a270d06310011c49c042a` |
| codex plugin.json | **version `0.2.5`** blob `2d71370bfbc3de1c6880bb964ac43acf29ee66c3` |
| marketplace.json | plugin **version `0.2.5`** blob `085064bb93d7314ed3509277939799732e31e297` |
| skills dirs | **6:** `cross-session-orchestration`, `source-battery-meta`, `source-frontier-battery`, `source-levers`, `source-mcp-usage`, `source-skills-manifest` |
| command files | **7:** `source-ask.md`, `source-battery-meta.md`, `source-frontier.md`, `source-health.md`, `source-levers.md`, `source-search.md`, `source-skills-manifest.md` |
| `prepare_connector_delta.py` | **absent** from `the-source` (search total_count=0) |
| `plugin_lock.py` / `MANIFEST_LOCK.json` | present |

Packet expected **4 skills / 5 commands / base 0.2.4 / candidate 0.2.5**. Origin **occupied 0.2.5** and **moved inventory**. Law: incompatible base → **reconcile, not downgrade**; **do not reuse an occupied release version**. If the stager still encodes 0.2.4→0.2.5, **refusal is the correct EX12 product** until rebound (free version ≠ 0.2.5, inventory = actual 6/7 or an owned delta — do not silently strip to 4).

Thin-front **intent** still holds on *new* additions: do not add a **fifth-beyond-actual** skill, command, hook event, router, workflow, timer, or PreToolUse. Do not copy the operator archive into the public plugin (**EX64**). `contracts/SKILLS_MANIFEST.json` and `HOOKS_CONTRACT.json` are review inputs, not host registration.

### 2.3 Exact command — CMD02 (EX12). No apply.

After EX02 closes on **this** worktree:

```bash
python3 inherited/SOURCE_PLUGIN_CONNECTOR_META_2026-09-17/tools/prepare_connector_delta.py \
  --repo /absolute/path/to/the-source \
  --expected-head FULL_CURRENT_HEAD \
  --out /absolute/path/outside-repository/source-plugin-meta-candidate
```

- `FULL_CURRENT_HEAD` = `git rev-parse HEAD` of **this** worktree after bind, not a pasted origin SHA if the tree has moved.
- `--out` **outside** the repository.
- Stager **has no apply option**. Do not force past refusal with fake metadata.
- CMD01 (packet validators) is local-packet only; its pass is not Source acceptance.
- CMD03 `python3 plugins/source/scripts/plugin_lock.py --record` is a **later** release mutation — not this increment. Read actual script/tests first.
- Native host install/uptake only with actual authorization (EX19–EX21). Copied Claude installs ≠ in-place Codex loads.

Allowed candidate contents (from owner relay; reconcile against **actual** 6/7):

- Compact supplement to existing `source-frontier-battery/SKILL.md` and `source-mcp-usage/SKILL.md`
- Dated references under frontier `references/connector-meta/` (read-on-demand; not every-user prompt)
- Comments only at end of `frontier_battery.py`; **AST unchanged**; router still delegates to existing gateway; **no conferral inner loop**
- Version fields in both plugin manifests + marketplace **only if** a free version is allocated
- Lock recorder output only after content integration, under normal release procedure

### 2.4 Connector-only. Do **not** absorb these into the plugin.

| Native / other-owner | Why it stays out |
|---|---|
| Gate B8 / `pre-ingest-census.py` | Lives in unified **PreToolUse epoch** hook. Cold 2.68s vs 2.25s budget. Census that runs **is** B8. Sequence: B8 first, then **one** epoch carrying #4218. **Not** plugin SessionStart/UserPromptSubmit. |
| Epoch install `install_hook_epoch_persistent.py` | CMD04 prepare / CMD05 apply — exclusive release owner. Journal APPLYING → existing recover_install, not retry. |
| `/durable-oneshot-drain` | Temporal residual then ratchet. H11: bare command first; `GOAL_OVERRIDE=1` **only** on exit 2. Distinct from `deterministic-oneshot-runbook` (compile) and `source-frontier-battery` (wrapper). |
| Graph planes A Search / B embedded suite / C AGA / D Nornic / E corpus | Search: admitted reads + metadata allowlist; **no** inherited suite-write. Embedded suite is a **separate write-capable owner** (gds.*.write → SUITE_V3_TO_ROUTING). AGA: estimate→2GB refuse, `verify_session_connectivity`, **session delete on fail**, no `from_env`. Nornic: **do not** prefix `CYPHER 25` unless accepted; no `LET`/`FINISH`/`SEARCH` port. Vendor `/neo4j-*` = **HASH_BOUND_REFERENCE_ONLY**. |
| Pairs-only / Voyage / pgvector | Generation eligibility ≠ pair eligibility. Reuse existing PG cosine path; **do not** expression-index, FastRP, GNN, Aura vector-property copy, or new ANN for the 23-ID batch. EX41 type-cluster fixture **before** live MERGE. Quoted MERGE must not collapse provenance. Destination instance **5627c34b** ≠ inventory **2423a9f4**. |
| 3pass / conferral / graph-first selector / G05 | Long-path residuals until epoch exists. Not plugin hooks. Not packaging `master_plan_path.r2+`. |
| CIMD / OAuth plugin object | AS `client_id_metadata_document_supported=false`. DCR `registration_endpoint` null. PRM scopes = OIDC (`openid profile email offline_access`), **not** `source:public`. JWT application scope `source:public` — **do not send as oauth.scopes**. 401 has **no** `scope=`. Rank-1: do **not** stuff CIMD `oauth{}` into plugin. Form elicitation = HARD_GATE confirm only; URL = Casa/OAuth; **no form secrets** (SEP-1036); no form fallback if url mode missing. PKCE **S256** only. MCP 2025-11-25 ≠ 2026-07-28. |
| Battery meta | `census ⊥ select ⊥ fire ⊥ witness`. Never a second router. Never swallows conferral. |
| Plugin hooks | SessionStart + UserPromptSubmit only; fail-open; **silent** when epoch estate already runs. No `manifest.hooks` duplicate. No PreToolUse/Stop in the plugin. |

Harness helpdocs (PINNED, not proof of live install):

- Anthropic: https://code.claude.com/docs/en/plugins — `plugin.json` `name` required; `skills/*/SKILL.md` `description` required; `hooks/hooks.json` stdin JSON
- OpenAI: https://developers.openai.com/codex/concepts/customization — `AGENTS.md` + `agents/openai.yaml` quoted strings; `$skill-name` `default_prompt`
- xAI: https://docs.x.ai/build/features/skills-plugins-marketplaces — same `SKILL.md`; extra keys ignored; `user-invocable: true` must be the **literal boolean**

### 2.5 After a real patch exists (not this watching seat)

EX13–EX20: four-skill **intent** vs actual 6/7 inventory — reconcile, do not invent. EX09 (API result envelope) is in the expander because EX21 depends on it; **ownership stays API/result**, connector only consumes the identity/readback rule (catalog hash ≠ semantics; generation churned `dae69cc80dec0bee` → `3899f56bec0a46cc`; catalog `0e6cad304fb4e343` held; `deploy_sha` is `/api/build` never git HEAD; native join 42/42; HTTP **502** remains named #4259 consumer-test). EX21 host acceptance. EX64: do not copy this archive into the installed plugin.

Return (distinct fields): HEAD; owned diff; proposed/final plugin version; lock result **if** recorded; tests with **separate** scopes (packet synthetic ≠ native); installed-cache identity if performed; preserved pair/custody snapshot; every unresolved prerequisite.

Finished packaging is **not** finished Source.

---

## 3. Master insert — `da60e026` only (do not run from the plugin seat)

**First cut: EX03** — admitted-client readback of already-served D₂ effects **before** conditional API release.

Observe, via the **actual admitted** route/test contract (bind a harmless or existing test case):

- multi-trigger `fullsend /x` (#4250)
- async-handle (#4260)
- CAP-004 READ-class (#4125)

Zero keystrokes is **not** a zero-effect guarantee. **Do not** replay `fullsend /x` merely because it appears here. 10:34Z rejection and served ancestry are **not** a fresh behavior result. HTTP 502 remains the named #4259 assertion. Current activation remains NOT_VERIFIED until this check.

Then, only if EX03 actually binds: EX07–EX11 result/envelope; EX22 conditional API (#4259+#4265 after recovery-lock; #4267 if qualified) — **not** the B8/#4218 epoch; EX23 client breadth; EX34/EX35 pair only under **live** custody. Do not transfer D₁ REFUSE onto D₂. Do not emit `master_plan_path.r2+` from packaging.

Delivery chain: accepted implementation → exact deployment where applicable → real admitted client use → independent readback of the useful result → next step from **that** result.

---

## 4. Other first cuts (retained, not this increment)

| Lane | First cut | Do not |
|---|---|---|
| corpus | EX41 type-cluster fixture, then EX39 pairs-only reuse of existing PG vectors | new embeddings, FastRP/GNN, expression index, Aura copy |
| epoch | B8 census out of PreToolUse budget, then one sealed epoch with #4218 | plugin hooks; `pre-ingest-census.py` as the census that runs |
| operator | EX57 rotate committed pooler credential (security **floor**, above ranking) then EX58 source-mcp 0.6.4→0.6.6 bind **running** launcher/cache | equate plugin 0.2.x with local mcp 0.6.x |
| selector / graph-econ | EX61 / EX62 after epoch exists | treat 13-leg PASS as arm evidence |
| learning G05 | grader attribution then graph-dark skills, then hold-out | plugin battery as grader |

Susie ranking (operator work, **not** the 13-pt ring): credential floor → source-mcp 0.6.6 → epoch → observer scheduler → Codex window coexistence → B8 census. G05 and graph-first score low because the **path is long**, not because they do not matter.

---

## 5. Anti-join (do not smear)

- `/unified-router` = SELECT, not terminal
- `/recursive-conferral` = selected 3pass ⊥ confer; not an activation inner loop; **not fired by the wrap**
- `/alignment-13pt` = wrap + ceiling; does not replace a confirming round
- `/durable-oneshot-drain` ≠ this file ≠ compile runbook ≠ frontier battery
- plugin version ≠ source-mcp version ≠ API revision ≠ sealed epoch
- Nornic ≠ Aura GDS ≠ Search ≠ embedded suite writes
- generation eligibility ≠ pair eligibility
- OIDC scopes ≠ `source:public`
- connect-time OAuth ≠ elicitation; form ≠ URL
- 2025-11-25 ≠ 2026-07-28
- B8 ≠ pre-ingest-census.py ≠ plugin hooks
- battery meta ≠ second router
- finished packaging ≠ finished Source
- absences ≠ zeros; `ok_count` ≠ independence
- local ICE ≠ organizational path-return — do not average
- clean outer JSON ≠ complete nested result
- Discovery / FTS / embeddings / graph **never** imply HIGH_STAKES_ELIGIBLE
- 173 tasks + R2 denominators are inherited — **do not add**
- catalog hash is **not** capability-semantic identity

---

## 6. Stop

- No fifth-beyond-actual skill/command/hook/router/timer from **this** packet
- No CIMD oauth object while AS CIMD is false
- No form secrets
- No switching registrations to bypass `PROFILE_ROUTE_DENIED`
- No sampling for conferral (host LLM is not an independent provider)
- No MCP tasks replacing `source_events`
- No resources/subscribe before `listChanged`
- No relabel DEFERRED → CONVERGED
- No FastRP/GNN/expression-index as the 23-ID pairs repair
- No combining the four releases’ versions, deploy IDs, or success claims
- Preserve exact failed products; do not retry an unchanged refusal; do not transfer ownership from a document

---

## 7. Return shape (when the selected owner actually runs)

1. tested / current revision (full HEAD)  
2. owned diff (paths + identities)  
3. expected vs observed product  
4. complete errors / partials / unresolved IDs  
5. actor / scope / principal  
6. applicable target readback  
7. required independent observation  
8. next action **from that result**  
9. which EX closure ran; which cards remain unexpanded  
10. which release artifact this was (plugin | source-mcp | API | epoch | none)

**No sacrifices** = no silent loss of supported goal, original qualifications, positive capabilities, owner boundaries, useful deferred possibilities, or required evidence.

---

**Watching seat ends here.** Source was not written. Selected owner: bind checkout, then the first unmet cut above.  
This cohesive file: https://raw.githubusercontent.com/shaneatlas/chamber-subsumption-2026-09-17/main/ONESHOT_COHESIVE.md

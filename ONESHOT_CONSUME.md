# CONSUME — core prompt + connector insert + live first cuts

**Prepared:** 2026-09-17T20:05Z watching seat (Grok Build Chamber).  
**Does not:** grant authority, write Source, fire conferral, apply, install, or change alignment.  
**Does:** give the selected owner one paste-ready contract. Graph/learning/watcher inserts stay **qualified handoffs**, not plugin work.

**Supersedes (inventory / first cut only):** every `0.2.4 → 0.2.5` / `4 skills / 5 commands` / **CMD02-stager** anchor in the Chamber wrap, `ONESHOT_COHESIVE.md`, `CONNECTOR_OWNER_ONESHOT.md`, and #4288’s CMD02 command. Those files remain lookup for EX card bodies and anti-join; **live base below is authority**.

Wrap (lookup, not re-hashed here): https://raw.githubusercontent.com/shaneatlas/chamber-subsumption-2026-09-17/main/ONESHOT_DYNAMIC.md sha256 `57457fd32f29beae27c38f2932f2cf7f519395915616b151c51529681185939c`

---

## 0. One law, one shape

A remedy whose own precondition cannot hold in the regime it exists for is not a step.

Before any step: *can this step's precondition hold in the state it is meant to fix, and who is paged if it silently does nothing?*

**Breadth across the objective. Depth at consequential uncertainty. Protected capacity within the wave. Decisive transitions between stages. Independent evidence at acceptance. Learning from observed results.**

Optimization target: **supported useful progress** — not maximal invocation, the smallest-looking patch, a predetermined approval, or an attractive completion label.

Protect delivery by managing scope, sequencing, and capacity — **not** by changing what counts as true, safe, authorized, or complete.

WIP=1 per owner. Four releases stay four: **plugin · local MCP install · API deploy · hook epoch**.

---

## 1. Bind once

```
if no genuine checkout + dirty/preimage + owner lease:
    WATCHING → do not author 0.2.6, do not EX03, do not confer, do not apply
```

| You are | First unmet cut (this wave) |
|---|---|
| **Connector `6310327d`** (claimed 16:00Z by `9403ebc2` — bind current holder) | Author the approved **0.2.6** reference delta on the live 0.2.5 / 6 / 7 base. **Not CMD02** (stager UNAVAILABLE). |
| **Master `da60e026`** | EX03 admitted-client readback. Not conferral. Not D₁ REFUSE as a D₂ verdict. |
| Disk / host operator | Remaining **host-config** packets (belt plist + intake-guard adoption). Producer #4327 already merged. |
| Result completeness (peer) | `source-mcp/protocol/turn.py:326` 4000-char silent slice. |
| Product-answer receipt (unowned) | Do not start from a packaging seat. |
| FGCS | **Already implemented.** Do not start Phase 0. |
| Watching | This file. No Source mutation. |

---

## 2. Live base (revalidated this seat at consumption — origin HEAD, not dirty)

| Surface | Observed now | vs runbook `e6802946ae` |
|---|---|---|
| `origin/HEAD` | **`ecd94a4dacdf429de79f70ab3815b965d4a9f95c`** (#4327 intake guard, 19:56:10Z) | **moved** — bind this SHA, do not freeze `e6802946ae` |
| Plugin `source` | **0.2.5** blob `a6177e7eb1ec354cd44a270d06310011c49c042a` | unchanged occupied |
| Skills / commands | **6 / 7** (includes `source-battery-meta`, `source-skills-manifest`) | unchanged |
| `connector-meta/` | **0** (code search total_count=0) | falsifier still open |
| `prepare_connector_delta.py` | absent from `the-source` | CMD02 remains UNAVAILABLE |
| Alignment | 3pass `4246cd7c` **DEFERRED** · confer #18772 **REFUSE** on D₁ · ceiling **EVIDENCE_INCOMPLETE** · no D₂ pair · r2+ `da60e026` · wrap `conferral_fired: false` (does not erase R1) | carried, not re-fired |

Your `git rev-parse HEAD` in the **owner checkout** is the patch expected-head. Origin SHA is not dirty.

---

## 3. Core prompt — existing Source execution owner

Continue the existing workstream from its current evidenced state. Preserve the larger Source objective: model- and host-independent access to accumulated intelligence, precise capability execution, durable continuity, and qualified learning. Do not restart the retrospective or create another router, registry, control plane, review framework, or backlog merely to enforce this prompt. The connector remains an access surface; native Source owners retain their authority, state, execution, recovery, and acceptance responsibilities.

Bind the useful behavior for the identified consumer, the first unmet acceptance, existing owner and task/run, actual candidate/target identities, current authority, pending operations, and next eligible action. Reconcile existing effects before repeating work. Use the existing release path and real scheduling facts; preserve unknowns rather than inventing a deadline. Carry the exact source/reference, relevant revision or receipt, prerequisite, result consumer, and stop or reentry condition with each consequential action in the existing record.

Use the actual `/unified-router` contract to consider the full eligible repertoire, **including capabilities not explicitly named in this message**. Select the sufficient complementary composition for the objective; neither maximum invocation count nor minimum skill count is the target. Preserve required children and independence. Bind capability, depth/modifier, evidence/state, and executor to the actual dispatch and result consumer. Distinguish rule selection, learned selection, operator override, required composition, and fallback. Keep the router cheap; continue an adequate route and reroute only when the contract's decision frontier, prerequisite, residual, owner, or target conditions change.

Within the existing owner's objective and resource boundary, **advance the work with the greatest supported impact relative to its whole time, effort, and dependencies**. Consider usable value to admitted clients and the outcomes a prerequisite enables. Preserve the established program order unless the owning process authorizes a revision; a local score cannot take another owner's release slot. Execute an already-supported authorized next action instead of asking another panel whether to begin.

Use `/recursive-conferral` and other review only where the actual contract requires them or a named consequential uncertainty justifies them. Bind the candidate, question, evidence, required protocol, stopping condition, cumulative cost, and action each material result enables. Preserve genuine children, sequential cross-feeding, and independence; eliminate duplicate reassurance. A refusal requires repaired facts or missing evidence before reconsideration, not changed wording. A shared authorized probe can proceed while the larger pair remains incomplete. **Conferral cannot create authority.** Discharge only the decision gates the installed protocol is authorized to discharge.

Protect the complete path through implementation, required tests and review, integration, authorized release, independent readback, consumer acceptance, and recovery. When an actionable result arrives, consume it in the next owned operation. Advance independent compatible work while genuine gates remain pending. Do not turn a timeout into approval, alter a frozen candidate invisibly, create a competing writer, bypass a denial through another interface, or sacrifice a genuine requirement to an arbitrary daily quota.

Close obligations only against their actual acceptance evidence. Keep intended, selected, invoked, returned, consumed, effective, and durable states distinct. Where a mechanical rank is requested, apply the actual ladder (`governance.fn_3ortho_actualized`); claim rank 3 only if its required proof exists. Preserve blocked, deferred, adverse, and learning residuals with owner, exact unmet predicate, and reentry condition. Do not delete them or call them drained because a smaller checkpoint shipped.

Record the actual route and executed fallback, outcome, independent quality/effect evidence, limitations, and justified successor through existing durable paths. **Verify later eligible consumption before claiming improved learning.** Return the consumer outcome, exact candidate/release identities, decisive source and readback references, highest proven state, residuals, and next owned action.

**Do not respond to absent runtime evidence by buying more opinions. Clear a gate, then take the next eligible step. Do not restart justification without cause.**

---

## 4. Connector-only insert — `6310327d` / current holder of that row

Apply the core prompt **only** within packaging scope.

### 4.1 First cut (runbook 2.2 — CMD02 is not the cut)

The prep package is **UNAVAILABLE**. Author the approved content delta as **plugin 0.2.6** on top of #4272 / live 0.2.5:

- 8 Markdown + 1 dated JSON under `plugins/source/skills/source-frontier-battery/references/connector-meta/`
- Compact supplements to existing `source-frontier-battery` and `source-mcp-usage` SKILL.md
- Comments-only change to `frontier_battery.py` (**AST unchanged**, no conferral inner loop, still delegates to existing gateway)
- Version field **0.2.6** in both `plugin.json` files **and** marketplace.json, then `plugin_lock.py --record` **after** reading the present lock-script contract and integrating content
- Preserve existing SessionStart / UserPromptSubmit only; fail-open; **silent** when epoch estate is detected (including detected-but-unhealthy)

**Preserve the live inventory:** 6 skills (`source-mcp-usage`, `source-levers`, `source-frontier-battery`, `cross-session-orchestration`, `source-battery-meta`, `source-skills-manifest`) and 7 command files. **Do not downgrade to 4/5.** If inventory moved again in your worktree, reconcile that exact delta.

**Do not:** reuse 0.2.5; add a seventh-beyond-actual skill; native CLI skills; PreToolUse/Stop; B8/epoch machinery; conferral execution; Nornic/pairs/selector consumers; extra gateway; timer; new controller; copy the operator archive into the installed plugin (EX64). Describing a capability is not packaging or activating it.

**Do not run:**

```bash
python3 inherited/SOURCE_PLUGIN_CONNECTOR_META_2026-09-17/tools/prepare_connector_delta.py ...
```

That command’s precondition (stager present, expected 0.2.4) **cannot hold**.

### 4.2 Acceptance (separate levels — do not collapse)

1. package / content checks  
2. `pytest plugins/source`  
3. `plugin_lock.py --record` readback  
4. installed-cache identity  
5. loaded-host skills/commands  
6. gateway catalog/schema identity  
7. on-demand reference consumption  

**Falsifier:** `git ls-tree -r origin/main plugins/source | grep -c connector-meta/` still `0` after “shipped”.

Keep plugin 0.2.6, local MCP 0.6.x, B8/#4218 epoch, and D₂ API as **four artifacts**. Package D₁ (`DEFERRED` / `REFUSE` / `EVIDENCE_INCOMPLETE`) as qualified references, never as a startup plan or an instruction to re-fire.

Where install authority is absent: return the exact tested/lock-recorded state and the installation residual. **Finished packaging is not finished Source.**

OAuth: do **not** stuff CIMD `oauth{}` into the plugin (AS CIMD false; DCR null; PRM = OIDC; JWT `source:public` is not an authorize scope). Form ≠ URL elicitation; no form secrets (SEP-1036); PKCE S256.

HTTP 502 remains the named #4259 consumer-test. Catalog hash ≠ semantics.

---

## 5. Other lanes — first cut only (not plugin work)

**Disk / host (operator packets; agent-denied class):**
- Repoint reclaim belt to the **guarded** copy and reload launchd (runbook 2.1 exact `plutil` / `launchctl` packet).
- Adopt #4327: `cp` merged `.claude/hooks/session-worktree-provision.py` over `~/.claude/hooks/…`; bootstrap merged plist. Readback: `worktree_intake_guard.jsonl` line whose `caller` is a real hook, not `smoke-*`.
- Falsifier: `~/.thesrc/reaper.disable` older than 12 h with no `reaper_kill_switch_alarm.jsonl` line.

**Master `da60e026`:** EX03 on production (`/api/build` == main, real route 401 / fake sibling 404). Ancestry is not consumer behavior. No blind `fullsend /x`. Falsifier: claiming D₂ without a new `governance.witness_receipt` whose `decision_text` names the D₂ candidate. Verdict of record is `MAX(created_at)` in `governance.witness_receipt`, never the by_id file. `apex=PASS + polarity=refuted` is not a pass.

**Result completeness (peer):** `turn.py:326` silent `json.dumps()[:4000]` → typed continuation or `truncated` flag; tests 3999/4000/4001 + multibyte; reconstruct in a real client. Falsifier: witness 18994 via MCP still unparseable nested JSON with a clean outer envelope.

**Product-answer receipt (unowned):** `/api/answer` edges with no operation receipt. Do not mint UUID from edge id. Do not start from this packaging seat.

**FGCS:** 36/36 registries, 0 trigger gaps, 42/42 skills. Residual is close-gate visibility + accidental `fn_fgcs_close_contract` owner. **Do not start Phase 0.**

---

## 6. Qualified references — native graph/learning and watcher (do not append as packaging)

**Graph-first:** keep `GRAPH-VECTOR-FRONTLOAD-REDUCES-RESIDUAL-REASONING-TAX:v1` as a **hypothesis under test**. PATH A reported landed; PATH B spec-only. A graph-shaped query is not useful graph-first reasoning. Bind discriminating question, qualified result, consuming decision, task effect. Do not bypass the lease. Generation eligibility ≠ pair eligibility; no FastRP/GNN/expression-index as the 23-ID pairs repair. Search ≠ embedded-suite writes ≠ AGA ≠ Nornic. Vendor `/neo4j-*` = HASH_BOUND_REFERENCE_ONLY.

**Learning join (actual order):** intended skill → selected skill → actually executed skill/fallback → attributable qualified outcome → canonical durable update → evaluated candidate → authorized admission → eligible later consumer → independently assessed benefit. Do not self-grade. A weight update the selector never reads is not useful.

**Watcher (if optional work repeats unchanged evidence):**  
> Preserve the valid evidence for the bound candidate. Defer or curtail the nonessential activity. Execute the next eligible action toward the identified consumer checkpoint. Keep the exact genuine gate, owner, and resolution condition visible. Record what changed in execution.

Do not seize custody. `/loop` cadence is not proof of persistent execution. Telemetry must show selected/invoked/failed/pending/effective work — not command-name chips.

---

## 7. Corrections that control execution

1. Consider broadly under `/unified-router`; select sufficiently; reroute only on frontier / prerequisite / residual / owner change — **never on a timer**.
2. Conferral manufactures no release, credential, migration, or cross-session authority.
3. A classifier denial binds every seat; hand the operator the exact command + readback. Do not re-form it. Attempt likely-denied actions **last**.
4. Merge ≠ deploy ≠ consumer effect. Name which checkout each launchd/plist actually runs.
5. OAuth: scope advertising ≠ registration ≠ enrollment ≠ consumer success. `resources/subscribe` and `listChanged` are independent.
6. Rank 3 only against the installed ladder.
7. Learning only after a later eligible consumer demonstrably changed behavior.
8. Counts keep their scope: 35+24 synthetic ≠ native; 10/13 with 4 absences ≠ one population until the roster is read.
9. Clean outer JSON ≠ complete nested result.

---

## 8. Return format (every lane, every stop)

```
owner / seat · bound HEAD · candidate/release ids
effect witnessed (surface + query) · highest proven state (SELECTED_NOT_INVOKED … DURABLE)
first unmet acceptance · exact next action · denied actions with the operator packet
residuals (blocked / deferred / adverse / learning) with owner + re-entry condition
```

Never finish with only another plan when an authorized action exists. Never claim an action when only this document was produced.

---

**This watching seat:** published this consume file. Did not author 0.2.6, lock-record, EX03, confer, apply, or install.  
**Next owned action:** current holder of `6310327d` authors 0.2.6 on `ecd94a4dacd` (or their worktree HEAD if moved). Falsifier remains `connector-meta/` count 0 on origin.

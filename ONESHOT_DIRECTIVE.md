# No-sacrifice oneshot directive

**Prepared:** 2026-09-17T13:50Z  
**Ingress:** `/unified-router` SELECT  
**Wrap:** `/alignment-13pt`  
**Conferral:** **DO NOT FIRE.** Round 1 already exists. This seat wraps to EVIDENCE_INCOMPLETE, then stops.  
**Claim ceiling:** `DIRECTIVE_ONLY · NOT_REPOSITORY_IMPLEMENTED · PAIR_EVIDENCE_INCOMPLETE`  
**WIP:** 1 per owner and contended surface. Four releases stay four.

Raw: https://raw.githubusercontent.com/shaneatlas/chamber-subsumption-2026-09-17/main/ONESHOT_DIRECTIVE.md

Packet (evidence, not this directive): `SOURCE_CONNECTOR_BUILD_RECOMBINED_2026-09-17.zip`  
sha256 `f0d88ce50864e392ac7242cc2ba4bc1d96fcb4ab769c091b00f8d48220200f84` · 162 files · local 35+24 tests only.

---

## 0. What this subsumes (nothing silently dropped)

| Plane | Bound as | Not converted into |
|---|---|---|
| Chamber 13-pt ring | teaching pin **#18767** 10/13; four *absences* | latest conferral, ok_count, independence |
| Latest conferral | #18769 trap / later receipts | the 13-geometry |
| Susie ranking `db4f4362` | operator ICE hand-off | conferral or plugin lock |
| Master path `9403ebc2` D_2 | r2+ **da60e026** | this oneshot’s writer |
| Thin plugin 0.2.4 → candidate 0.2.5 | **6310327d** CMD02 | epoch, API, local source-mcp |
| OAuth / elicitation | PRM OIDC ≠ JWT `source:public`; CIMD **false**; DCR **null** | stuffing CIMD `oauth{}` into plugin |
| Graph 3-ortho | PG embeddings authority · Aura topology · Nornic mirror | vendor `/neo4j-*` execution |
| Pairs-only R4 | `enrich_with_voyage.py` selection split | re-embed, FastRP, GNN, expression index |
| Battery meta | census ⊥ select ⊥ fire ⊥ witness | second router |
| Helpdocs | Anthropic / OpenAI / xAI pinned | slash interception via MCP |
| Signal-13pt / engineering-13pt | protocol lattice + sense→actuate map | public authority widen |
| Recombination S01–S38, B01–B07, EX01–EX64, L0–L5 (72) | navigation + owner cards | new task rows, new ICE rank, plugin dump |
| 94 namespaced acceptances | retained | 64 new tests |

**“No sacrifices”** = no silent loss of the supported goal, original qualifications, positive capabilities, owner boundaries, useful deferred possibilities, or required evidence. Optional cost/latency/quality choices still need a declared workload and actual comparison. An authorized negative is useful; it is not a positive completion.

**Delivery chain (unchanged):** accepted implementation → exact deployment where applicable → real admitted client use → independent readback of the useful result → next step from that result. A zip, a 13-leg receipt, a healthy API, or a workflow is evidence, not the chain.

---

## 1. `/unified-router` decision (this invocation)

```
route:              new
objective:          Unify the recombined connector packet under alignment-13pt
                    and emit one granular oneshot. Do not confer. Do not apply.
sufficiency:        Packet is LOCAL_REFERENCE_PACKAGE_VALID.
                    Pair remains EVIDENCE_INCOMPLETE.
                    This surface cannot mint 3pass, confer, lock, or install.
change_conditions:  da60e026 confirming pair on D_2;
                    or 6310327d CMD02 patch exists against current HEAD;
                    or EX03 falsifies D_2 step 1.
next_action:        Owners consume their lane below. Chamber stays atlas.
not_established:    PAIR_ALIGNED, 0.2.5 installed, native uptake,
                    #4259+#4265 client behavior, learning reentry.
learning_link:      none — this seat cannot mint execution_outcomes.
```

Anti-join (do not smear):

- `/unified-router` = SELECT, not terminal  
- `/recursive-conferral` = selected 3pass ⊥ confer; **not** an activation inner loop  
- `/alignment-13pt` = wrap + ceiling; does not replace the confirming round  
- `/durable-oneshot-drain` = Temporal residual then ratchet; **not** this document  
- plugin 0.2.4 ≠ local source-mcp 0.6.x ≠ API revision ≠ sealed hook epoch  
- Nornic ≠ Aura GDS ≠ Search  
- generation eligibility ≠ pair eligibility  
- OIDC scopes ≠ `source:public`  
- connect-time OAuth ≠ elicitation  
- form HARD_GATE ≠ URL elicitation (SEP-1036)  
- 2025-11-25 ≠ 2026-07-28  
- B8 ≠ `pre-ingest-census.py` ≠ plugin hooks  
- finished packaging ≠ finished Source  

Default selection rule: greatest supported impact / total effort on the **selected owner’s** surface, then update from the observed result.

---

## 2. `/recursive-conferral` until `/alignment-13pt`

**Do not invoke native 3pass or confer from this directive.**

| Record | Identity | Result | Use |
|---|---|---|---|
| Publication R0 | #18770 / `288a70f6-be42-4673-8755-a79df1b31ace` | SUSTAIN 10/13 | publication only; cannot stamp `bash:git-mutation` |
| Formal audit r1 | `4246cd7c-d64e-5021-8ca8-535c386a01bd` | DEFERRED (last Δ=4; empty caches) | not CONVERGED |
| Confer r1 on **D_1** | #18772 / `37ac62ee-3562-4142-b414-da8b5c9c55f7` | REFUSE/refuted 11/13 | does not judge D_2 |
| Pair | Source DISAGREEMENT | effective **EVIDENCE_INCOMPLETE** | wrap stop |
| Rounds 2+ | owner **da60e026** | not emitted | EX34 / EX35 only under live custody |

Alignment-13pt stop rules:

1. PAIR_ALIGNED requires a confirming next pair on **unchanged hash**, termination class, and recommendation, with no remaining decision-changing conflict.  
2. DEFERRED cannot be relabeled CONVERGED (symmetric-set identity, not cardinality).  
3. `CAUGHT_OVERCLAIM` / refuted polarity is refusal even at 13/13.  
4. `DOMINATOR` is its own formal state.  
5. Geometry objects stay separate: runtime 9→3→1 ≠ TDT 4-dim ≠ shape-ladder k=13 ≠ skill_13pt_meta ≠ Aura Skillset13 ≠ Chamber layout ≠ continuity 2-leg.  
6. Teaching ring stays **#18767**. Latest 2-leg flood is a trap, not the geometry. Absences ≠ zeros.

If a later seat *selects* conferral, feed each partner the other’s recommendation, objections, evidence, and receipt under the **same decision identity**. Revision changes the identity. D_1 results do not certify D_2.

---

## 3. Four releases (never one artifact)

| Artifact | Requirement | Owner | Do not equate with |
|---|---|---|---|
| Plugin package | 0.2.5 only after actual 0.2.4/current allocation bind | 6310327d | local source-mcp, API, epoch |
| Local source-mcp | reported 0.6.4→0.6.6; bind running launcher/cache | operator EX58 | plugin version |
| API release | D_2 #4259+#4265 after recovery-lock; #4267 if qualified | da60e026 EX22 | B8/#4218 epoch |
| Sealed hook epoch | B8 in candidate; #4218+B8 via installer; stamper if ready | hook estate EX25–EX30 | editing HEAD, plugin bump |

Independent reads are not a fifth mutation slot.

---

## 4. Granular lanes (consume one)

### Lane PLUGIN — first if you are 6310327d

**Cards:** EX02 → EX12–EX21 → EX64  
**Commands:**

```bash
# CMD01 already ran in the packet (local only). Do not treat as Source.

# CMD02 — preparation only, no apply
python3 inherited/SOURCE_PLUGIN_CONNECTOR_META_2026-09-17/tools/prepare_connector_delta.py \
  --repo /absolute/path/to/the-source \
  --expected-head FULL_CURRENT_HEAD \
  --out /absolute/path/outside-repository/source-plugin-meta-candidate
```

**Inventory (do not grow):** skills `source-mcp-usage`, `source-levers`, `source-frontier-battery`, `cross-session-orchestration`. Commands `source-ask`, `health`, `search`, `levers`, `frontier`.  
**Permitted payload:** `frontier-skill.append.md`, `mcp-usage.append.md`, comments-only helper, `references/connector-meta/*`, version fields when 0.2.5 is free.  
**Hooks:** SessionStart + UserPromptSubmit only. Estate detection suppresses plugin startup — suppression is precedence, not health. **Do not copy Gate B8.**  
**Lock:** CMD03 `plugins/source/scripts/plugin_lock.py --record` only after reading the **current** recorder and integrating the reviewed patch. Local SHA256SUMS ≠ native lock.  
**Milestones stay four:** prepared patch → integrated/locked candidate → supported installation → actual native use.  
**OAuth (measured 2026-09-17):** do **not** add CIMD `oauth{}` while AS `client_id_metadata_document_supported=false` and `registration_endpoint=null`. Advertise `source:*` on PRM or 401 `scope=`, or pre-register Claude/Codex client_ids. Request OIDC scopes; mapper yields JWT `source:public`.  
**AlwaysLoad / enabled_tools / allowed_tools:** `source_catalog_identity`, health, capabilities.  
**EX64:** do not ship this operator archive as public plugin content.

### Lane MASTER — first if you are da60e026

**Cards:** EX03 (step 1) → EX22 → EX07/EX08 as implicated → EX34/EX35 only with live pair custody.

D_2 order (dated; do not collapse):

1. Already-served effect readback: multi-trigger (#4250), async handle (#4260), CAP-004 READ (#4125) through the **admitted client**. No blind `fullsend /x`. Ancestry at `61a3e6a6…` included those PRs; only #4259/#4265 were reported open. Ancestry ≠ user-facing behavior. 10:34Z rejection predates that build.  
2. Conditional API release: #4259 + #4265, #4267 if qualified, after recovery-lock/status. HTTP 502 stays the **bundled consumer-test** for #4259, not a universal guarantee. Incomplete provider → `PROVIDER_ATTEMPT_UNRESOLVED` + ProviderError, not success.  
3. Client breadth: HAR-001, CAP-003 cache, PKG-003 CLI, `source.utility.evaluate` — product results, not proxy reward.  
4. First learning proof from **qualified client results**. Sept 17–21 is a risk window, not a pass.  
5. Deferred carrier: F001 then CAP-008/F075/F052 (historically Sept 23 actuators). Rebind custody.  
6. Operator hygiene: 9031 lease, pre-push gate, Casa sign-in — their owners. Casa is not a website in this connector.

**Do not** transfer D_1 REFUSE onto D_2. **Do not** emit `master_plan_path.r2+` from packaging.

### Lane CORPUS — graph-ingest owner, when that surface is selected

**Cards:** EX41 (fixture first) → EX39 → EX40 → EX42. L1.1–L1.28 live here.

Defect: generation eligibility currently gates pair eligibility (`--global-pairs` exits EMPTY_ALREADY_SATISFIED). Split:

```
requested IDs
  ├── generation candidates: missing/stale, if generation requested
  └── pair anchors: valid existing PG vectors, including already-embedded
```

Reuse-only: zero Voyage calls, zero `persist_embeddings`. MERGE identity includes generator + compatible model; reject ON CREATE SET-only. Keep weight/run_id out of MERGE. Stage directed; publish undirected after endpoints exist. Threshold after k; zero neighbors can be a complete search. Missing endpoint ≠ zero neighbors. Queue follows payload/handler, not n=23. Reader separates cosine / type-cluster / SHARES_FACT. DISCOVERY_ONLY:PARTIAL_DEFERRED stays until the product is actually complete. GDS skipped. No FastRP/GNN/expression-index in this repair.

### Lane EPOCH — hook estate, when that surface is selected

**Cards:** EX25–EX30. L0.3–L0.5.

Census that **runs** is Gate B8 in unified PreToolUse (`unified-pre-tool-gate.py`). `pre-ingest-census.py` never runs. Canonical task `cadc7a87`; close `444bbf35` and `bbea401e`. Cold 2.68s vs 2.25s budget. Sequence: B8 first, then **one** epoch carrying #4218; stamper `91d17c55` only if ready.

```bash
# CMD04 prepare (host-file read; inspect actual parser first)
python3 scripts/governance/install_hook_epoch_persistent.py \
  --epoch EPOCH_SHA --bundle BUNDLE_SHA

# CMD05 apply — exclusive release owner only
python3 scripts/governance/install_hook_epoch_persistent.py \
  --epoch EPOCH_SHA --bundle BUNDLE_SHA --apply --provenance PROVENANCE_DIR
```

Success ceiling: `REGISTRATIONS_WRITTEN_NATIVE_UPTAKE_UNPROVED`. `APPLYING` journal → existing recover_install, **not retry**. Epoch hash ≠ bundle hash ≠ runtime hash. 77 descriptors is the candidate check, not a law. Bare dispatcher defaults to shadow. Import probe ≠ worker polling.

---

## 5. Deferred on purpose (still in the map)

Do not insert these ahead of an already-bound useful repair. Do not drop them.

| When | Cards / L | Product |
|---|---|---|
| Operator floor | EX57 L0.1 | Rotate committed pooler credential; strip embeds. Not plugin enrollment. |
| Local MCP | EX58 L0.2 | source-mcp 0.6.6 on the **running** install |
| Recovery observer | EX59 L0.6 | Real scheduler; 7/7 rank-3 and 30/30 oneshot do not instantiate it |
| Codex window | EX60 L0.7 | Fixed-version scope vs daily deploys; owner 086cc4ec |
| Router native body | EX04–EX06 | Native namespace ≠ vendor `source.skills.catalog` hash |
| Result completeness | EX09–EX11 | Clean outer envelope ≠ complete nested JSON |
| Search / rerank | EX43–EX45 L2 | ADVISORY_ONLY ≠ no call; never store rerank as SIMILAR_TOPIC cosine |
| Metrics | EX49 L3 | ANN recall ≠ candidate recall ≠ nDCG; exact GT over eligible population |
| Learning | EX48–EX51 L5 | SLP-1 WP00–WP11; episode → candidate → independent eval → scoped admission → unseen consumer |
| Selector | EX61 | `graph_first_battery_select.py` — not a plugin hook |
| Graph-econ | EX62 | owner 1715404c; 13-leg PASS is not arm evidence |
| Resources | EX63 | parent+child reserve; timeout ≠ remote cessation |
| L4 later | EX55 L4.1–L4.7 | context-3, law-2, halfvec/int8, FastRP/GNN, dialect/AGA — **later products** |
| Harness pilots | EX52–EX54 | only for a demonstrated missing function; Source keeps custody |

Vendor `/neo4j-*`: HASH_BOUND_REFERENCE_ONLY. Dialect split is the harvest (Aura Cypher 25 / Nornic / AGA sessions), not 29 slash commands.

---

## 6. Battery meta (compact; not a dump)

```
census  → bind THIS registration (source_catalog_identity)
select  → capability / depth / evidence / executor under feasible authority
fire    → existing dispatcher and custody owner
witness → complete result + applicable target observation + actual reader
```

H11 oneshot flavors (do not merge):

- `/durable-oneshot-drain` — Temporal residual board then ratchet; bare command first; `GOAL_OVERRIDE=1` only on exit 2  
- `deterministic-oneshot-runbook` — compile  
- `source-frontier-battery` — router wrapper  
- **this file** — operator/developer directive after alignment wrap  

Helpdocs (pin, don’t paraphrase into law):

- Anthropic: https://code.claude.com/docs/en/mcp · https://code.claude.com/docs/en/plugins · https://code.claude.com/docs/en/hooks  
- OpenAI: https://developers.openai.com/codex/mcp · https://developers.openai.com/codex/guides/agents  
- xAI: https://docs.x.ai/docs/guides/tools/mcp-servers · https://docs.x.ai/docs/guides/grok-cli  

MCP initialize here is **2025-11-25**, `listChanged=false`, curated 42 / registered 188, catalog `0e6cad304fb4e343`. Catalog hash alone is not semantics.

---

## 7. Stop / refuse

- No fifth skill, command, hook event, router, workflow, or timer in the plugin.  
- No `manifest.hooks` if the package auto-loads.  
- No sampling for conferral (host LLM is not an independent provider).  
- No MCP tasks to replace `source_events`.  
- No resources/subscribe before listChanged.  
- No form secrets (SEP-1036).  
- No switching registrations to bypass `PROFILE_ROUTE_DENIED`.  
- No replay of effectful `fullsend /x` because the string appears here.  
- No reset of Source HEAD to a historical SHA.  
- No averaging of local ICE with organizational path-return.  
- No adding 173 + R2 300 task counts.  
- Preserve HTTP 502 as named #4259 consumer-test.  
- Preserve exact failed products; do not promote status or retry an unchanged refusal.

An actual denial, unknown effect, foreign config bytes, missing native consumer, changed authority, or absent executable contract remains explicit. Another lane’s unresolved work is not erased.

---

## 8. Return shape (when an owner actually runs)

Report: tested revision · owned diff · expected vs observed product · complete errors/partials · actor/scope · applicable target readback · required independent observation · next action from that result.

Distinguish: candidate · repository integration · deployed artifact · native/client consumption · accepted effect · qualified learning update · later use · assessed benefit. Do not let one status mean all of them.

---

**Next load-bearing action is owner-relative.**  
If plugin: **CMD02 on current HEAD, no apply.**  
If master: **EX03 admitted-client readback.**  
If corpus: **EX41 fixture, then EX39 split.**  
If epoch: **B8 out of PreToolUse, then one sealed epoch.**  
This Chamber seat: atlas + this directive. Source was not written.

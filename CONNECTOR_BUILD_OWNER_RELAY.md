# Connector build — source-linked owner relay

**Execution mode:** current-checkout, additive work through existing owners, current authority and release rules. The request to recombine these materials creates this handoff; it does not transfer another seat's mutation scope. No new task rows, universal review loop, generic broker or replacement registry.

## 1. Read the current decision, not the stale plan headline

Bind the relevant current owner and concrete useful checkpoint once using [EX01](SOURCE_CONNECTOR_BUILD.html#ex01). The recorded r2+ owner is **da60e026**. D_2's first stage is already-served behavior readback; conditional API release follows the relevant results and recovery-lock check. The plugin owner remains separately reported as **6310327d**, subject to current custody. [S01:L886–L892](source_views/S01.html#L886) [S01:L848–L856](source_views/S01.html#L848)

Do not revive a false four-fix unserved premise, transfer D_1's verdict to D_2, or claim a confirming pair. The formal audit remains DEFERRED and the effective pair ceiling EVIDENCE_INCOMPLETE in this record. R0 is publication-only. [S01:L827–L852](source_views/S01.html#L827)

## 2. Choose the owner's existing lane, not every card

For the plugin, consume EX02 then EX12–EX21 and EX64. For the API/result path, consume EX03–EX11 and EX22/EX23 as applicable. For the corpus product, consume EX41 first, then EX39, EX40 and EX42. B8/epoch uses EX25–EX30; it does not receive plugin, corpus or rerank payload by association. Native pair rounds use EX34/35 only under their actual decision/custody contract.

These groups are navigation, not permissions to execute unrelated work in parallel. Positive work must remain possible: an already-correct path should be demonstrated rather than patched for the sake of matching a checklist. Current instructions, genuine worktree, actual leaf schema and owner authority control.

## 3. Reuse the delivered preparation tooling, without upgrading its proof

[CMD01](COMMANDS_AND_BINDING_GAPS.md#cmd01) checks this packet. The inherited checker and thirty-five synthetic tests are in the unchanged inner package. [CMD02](COMMANDS_AND_BINDING_GAPS.md#cmd02) prepares a patch against an actual compatible checkout and full HEAD; it has no apply option. Do not replace actual manifests/hooks from prose or bypass a moved-version refusal.

The permitted plugin changes, exact files and lock boundary remain those of [S03](source_views/S03.html#L15). The current native lock recorder must be read before [CMD03](COMMANDS_AND_BINDING_GAPS.md#cmd03). Installation and actual cache/host/gateway readback are separate milestones. A native helper defect is an explicit scoped owner delta, not a silent expansion of the comments-only payload.

## 4. Let actual source support every execution step

For the selected EX card, open its cited source lines and bind the current implementation/caller, request scope, accepted input identity, authority, permitted effects and resource treatment. Run the positive and negative controls at the appropriate grain. Preserve exact result/receipt/target/reader identity. A `READ` label does not authorize unbounded inference; a successful outer response cannot certify truncated inner data. No fabricated flags or native endpoint names are permitted.

A negative fixture can pass while the system behavior it characterizes remains a defect. A successful document lookup can be the appropriate product without a mutation. A publication with no qualifying real pairs can be complete only when search itself completed and the positive writer fixture proves the alternate branch. The card's acceptance fits its product.

## 5. Preserve native runtime custody

The epoch wrapper is not the pin writer; the installer changes registrations, not epoch code or loaded sessions. `REGISTRATIONS_WRITTEN_NATIVE_UPTAKE_UNPROVED` remains its ceiling. Use the actual recovery contract for `APPLYING`, never another blind apply. Native reload and event observation establish uptake. Hook Temporal shadow/start/activity/readback states remain separate from general work recovery and residual boards. [S38:L11–L72](source_views/S38.html#L11)

## 6. Close the selected consumer before claiming improvement

Return exact tested/current revision, owned diff, expected and observed product, complete errors/partials, actor/scope, applicable target readback, required independent observation and the next action selected from that result. Distinguish candidate, repository integration, deployed artifact, native/client consumption, accepted effect, qualified learning update, later use and assessed benefit. Do not add one status or receipt to mean all of them.

For learning, preserve SLP-1 and original WP00–WP11, evaluation ownership and source-family independence. Do not award a reward to a non-executed provider, a fallback mislabeled Voyage, or a summary repeating the lesson. External harnesses and representation experiments are later/conditional products, not prerequisites inserted ahead of an already-bound useful repair.

## 7. Stop exactly at the real boundary

An actual denial, unknown effect, foreign config bytes, missing native consumer, changed authority or absent executable contract remains explicit. Preserve valid work and current custody; do not change credentials, switch identity, forge a hash, rerun conferral, drain a similarly numbered queue or copy native hooks into the plugin to get past it.

The selected lane ends at its actual accepted consumer result or an honest precise residual. Another lane's unresolved work is not erased. No full-system, 173/R2, r2+, PAIR_ALIGNED or learning closure follows from completing this artifact or its local checks.
